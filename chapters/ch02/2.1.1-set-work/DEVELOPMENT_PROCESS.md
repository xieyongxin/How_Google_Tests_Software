# 开发过程：模拟 Google 的 SWE 与 SET 协作

这份文档记录 Demo 的实际编写流程。它不是事后拼出的功能清单，而是与 Git 提交同步维护的工程日志。每个阶段都说明负责角色、交付物和质量验证方式。

## 角色约定

- **SWE（Software Engineer）**：设计接口、实现产品代码，并为自己负责的构建目标编写基础小型测试。
- **SET（Software Engineer in Test）**：参与设计，发现可测试性和覆盖缺口，建设 mock/fake，补充小型及中型测试。
- **构建工程角色**：把编译、静态分析和各层测试组织成所有人都能重复执行的统一入口。

这不是传统的“开发写代码、测试包办验证”。质量属于整个工程团队，SET 的价值在于提升团队的测试能力和工程效率。

## 实践阶段

| 阶段 | 角色 | 交付目标 | 验证 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | 团队 | 总结章节内容，确定实践路线 | 检查文档结构 | 已完成 |
| 2 | SWE | 定义接口和独立构建目标 | Maven 编译 | 已完成 |
| 3 | SWE | 实现订单服务和基础小型测试 | 单元测试 | 已完成 |
| 4 | SET | 补充边界测试和可复用 fake | 单元测试 | 已完成 |
| 5 | SET | 验证多个真实模块的协作 | 集成测试 | 已完成 |
| 6 | 构建工程角色 | 建立统一静态检查与测试入口 | `mvn verify` | 已完成 |
| 7 | 团队 | 打包、运行并完成章节验收 | 全量构建和命令行运行 | 已完成 |

## 阶段记录

### 阶段 1：明确目标

- 提交：`文档：整理SET工作要点与订单实践流程`
- 决策：每节使用独立工程，保留能逐步复盘的中文提交。
- 决策：采用接口先行；SWE 编写基础单元测试；SET 补充测试点、fake 和集成测试。
- 结果：章节总结、概念映射和七阶段实践路线已建立。

### 阶段 2：接口先行

- 提交：`开发：定义订单系统服务接口和构建目标`
- SWE 交付：库存与支付接口，以及四个边界清晰的 Maven 构建目标。
- 依赖方向：订单服务只依赖 `service-contracts`，尚不依赖任何具体库存或支付实现。
- 验证命令：`mvn clean compile`
- 结果：聚合工程与四个模块均可独立编译。

### 阶段 3：SWE 交付功能与基础测试

- 提交：`开发：实现订单服务并添加基础单元测试`
- SWE 交付：订单请求、处理结果和协调库存与支付的订单服务。
- 小型测试：使用 Mockito 隔离外部依赖，验证成功下单和库存不足两条核心路径。
- 验证命令：`mvn test`
- 结果：SWE 编写的 2 个单元测试通过，其他模块仍可正常构建。

### 阶段 4：SET 补充测试能力

- 提交：`测试：补充边界场景和模拟依赖工具`
- SET 评审：核心路径已有覆盖，但支付失败后的库存一致性、非法输入和禁止多余调用尚未验证。
- 补充测试：支付拒绝时释放库存；非法数量、金额和空请求不接触外部依赖。
- 测试工具：实现内存库存 fake 和可配置、可记录调用的支付 fake，并为共享 fake 添加自己的单元测试。
- 验证命令：`mvn test`
- 结果：订单服务 6 个小型测试、fake 服务 3 个小型测试全部通过。

### 阶段 5：SET 添加中型集成测试

- 提交：`测试：添加订单系统跨模块集成测试`
- 测试边界：使用真实 `OrderService`、内存库存 fake 和支付 fake，不检查实现内部的方法调用细节。
- 集成场景：成功下单会扣减库存并记录支付；库存不足不会支付；支付拒绝会恢复库存。
- 验证命令：`mvn -pl order-app -am test-compile org.apache.maven.plugins:maven-failsafe-plugin:3.5.3:integration-test org.apache.maven.plugins:maven-failsafe-plugin:3.5.3:verify`
- 结果：3 个中型集成测试通过，同时之前的 9 个小型测试继续作为回归测试运行。

### 阶段 6：统一工程质量入口

- 提交：`工程：接入静态检查和统一验证流程`
- 统一入口：Maven 生命周期依次执行 Checkstyle、编译、Surefire 小型测试、Failsafe 中型测试和 SpotBugs。
- 一致性：所有插件版本由父工程集中固定，所有子模块继承同一套规则。
- 验证命令：`mvn verify`
- 结果：Google Java 风格检查和 SpotBugs 无告警，9 个小型测试与 3 个中型测试全部通过。

### 阶段 7：组装产品并验收

- 提交：`应用：添加命令行演示并完成章节验收`
- 团队交付：命令行应用将独立构建的订单库、库存 fake 和支付 fake 组装成可运行产品。
- 打包方式：Maven Shade Plugin 生成包含运行时依赖和主类清单的 `order-demo.jar`。
- 验证命令：`mvn clean verify`
- 运行命令：`java -jar order-app/target/order-demo.jar`
- 结果：12 个测试全部通过；程序输出成功状态、非空确认号、剩余库存 3 和 1 次支付尝试。

## 最终回顾

这个过程刻意保留了构建目标逐步增长的轨迹：先有公共接口，再有经过小型测试的服务库，然后由 SET 增加测试工具和中型测试，最后才形成统一质量门禁和可运行产品。任何后续修改都必须重新运行已有测试，因此这些测试已经成为产品的一部分，而不是一次性的验收脚本。
