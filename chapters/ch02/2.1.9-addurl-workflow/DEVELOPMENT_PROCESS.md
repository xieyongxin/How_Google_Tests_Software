# 开发过程：AddUrl 协议到完整服务

本实验按书中 2.1.9 的顺序推进：先定义协议，再编译生成类型，随后实现服务、前端、fake 和分层测试。

## 角色约定

- **SWE**：实现协议消费方、AddUrlService 和核心小型测试。
- **SET**：审查协议字段和错误语义，提供 fake/延迟依赖，补充超时、兼容性及跨模块测试。
- **构建工程角色**：维护 Protobuf 生成、Surefire/Failsafe 测试入口和可执行 JAR。

## 阶段记录

| 阶段 | 角色 | 交付物 | 验证 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | SWE/SET | `addurl.proto` 和 Java 生成配置 | `mvn generate-sources` | 已完成 |
| 2 | SWE | AddUrlService、IndexStore 和存储实现 | `mvn test` | 已完成：提交 `5262be4` |
| 3 | SWE | AddUrlFrontend、查询参数解析和依赖注入 | `mvn test` | 已完成：提交 `43930ae` |
| 4 | SWE | 小型测试和 fake 后端 | 10 个小型测试通过 | 已完成：提交 `55c8f14` |
| 5 | SET | 边界、超时和协议兼容测试 | 14 个小型测试通过 | 已完成：提交 `11c2167` |
| 6 | SET | 中型、大型工作流测试 | 4 个中型/大型测试通过 | 已完成：提交 `82b25ae` |
| 7 | 团队 | 可执行 JAR 和章节验收 | `mvn clean verify`、`java -jar` | 已完成 |

## 当前协议决策

- 使用 `proto2` 体现书中 `required`/`optional` 字段和存在性语义。
- `url` 字段编号固定为 `1`，`comment` 字段编号固定为 `2`；后续字段只能追加新编号。
- 服务成功时不设置错误字段；客户端错误使用 400，后端失败使用 500，超时使用 504。
- 协议消息和服务端实现分离，前端只依赖后端客户端接口，不依赖具体存储。

## 实际复盘

- `baf9eac`：SWE 先提交协议、构建配置和生成类型测试。
- `5262be4`、`43930ae`：SWE 按接口实现服务和前端，保持每个 CL 可编译。
- `55c8f14`：SWE 提交基础小型测试和 fake。
- `11c2167`：SET 加入错误注入、协议兼容和文件边界测试。
- `82b25ae`：SET 加入中大型协作测试，团队完成可执行 JAR 验收。

每个阶段都先在本地执行验证，再以中文提交信息推送到 `main`，可以用 `git log --reverse` 重放完整过程。
